<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.ddu.oracle.dao.MemberDao" >
    	<insert id="memberJoinDao">
    		INSERT INTO membertbl(membernum, memberid, memberpw, membername) VALUES (MEMBER_SEQ.nextval , #{param1} , #{param2} , #{param3})
    	</insert>
		<select id="memberIdCheckDao" resultType="int">
			SELECT COUNT(*) FROM membertbl WHERE memberid=#{param1} <!-- 아이디가 존재 시 1을 반환 -->
		</select>
		<select id="loginOkDao" resultType="int">
			SELECT COUNT(*) FROM membertbl WHERE memberid=#{param1} AND memberpw=#{param2} <!-- 아이디가 존재 시 1을 반환 -->
		</select>
    </mapper>